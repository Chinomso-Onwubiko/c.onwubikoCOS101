fn main() {
    let interest: f32 = 8.0; //(fixed the error) integer is assigned to a float variable so this will throw an error.
    println!("Interest is: {}", interest);

    //alternatively, you can type cast by using the "as" keyword, or by using the built in method, I will look into that later).
}